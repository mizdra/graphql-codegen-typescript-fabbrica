{
  "name": "e2e-basic",
  "type": "module",
  "scripts": {
    "start": "run-s gen lint test",
    "gen": "graphql-codegen-esm",
    "lint": "run-s -c lint:*",
    "lint:tsc": "tsc",
    "test": "vitest --run"
  },
  "dependencies": {
    "@mizdra/graphql-codegen-typescript-fabbrica": "../.."
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
